/*******************************************************************************
@TEST_DESC@ Call console info/error fns
*******************************************************************************/

@INCLUDE
{

#include "cmap-log-ext.h"
#include "cmap-config-ext.h"

}

@STATIC_FUNCTION(test003)
{
  local test = function() {
    cmap.console.info("Hello ", "World ",  "!!!").info();
    cmap.console.info();

    cmap.console.error("[ERROR]");
  };

  local job = new cmap.scheduler.job(test);
  job.schedule();
}

@C_ROOT
{{

int main(int argc, char * argv[])
{
  cmap_config_set_log_lvl(CMAP_LOG_DEBUG);
  cmap_env_main(cmap_env(), test003);
  return cmap_main();
}

}}
